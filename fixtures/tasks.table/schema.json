{
  "fields": [
    { "name": "title", "type": "string", "constraints": { "required": true } },
    { "name": "project", "type": "string", "relation": { "table": "projects", "field": "id" } },
    { "name": "status", "type": "string", "constraints": { "enum": ["todo", "doing", "done"] } },
    { "name": "priority", "type": "integer", "constraints": { "minimum": 1, "maximum": 5 } },
    { "name": "assignee", "type": "string" }
  ],
  "primaryKey": ["title"],
  "schema-version": 1
}
