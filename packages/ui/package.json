{
  "name": "@workspace/table-ui",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "react-native": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@workspace/table-core": "file:../core"
  },
  "peerDependencies": {
    "react": ">=19.0.0",
    "react-dom": ">=19.0.0",
    "react-strict-dom": ">=0.0.55"
  },
  "peerDependenciesMeta": {
    "react-dom": {
      "optional": true,
      "comment": "Only needed by web consumers — used by createPortal in SchemaEditor + DragGhost. Native consumers (Expo, RN-macOS) don't install it. Drop once the Portal abstraction lands and these imports become conditional."
    }
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "react": "^19.2.0",
    "react-strict-dom": "0.0.55",
    "typescript": "^5.4.0"
  }
}
